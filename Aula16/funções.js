function parimp(n) {
    if (n%2==0){
        return 'Par'
    } else {
        return 'Impar'
    }
}
/*
let res = parimp(4) //é nessário colocar o prompt dentro de uma variavel~
console.log(res)

// Ou podemos podemos chamar diretamente na impressão
*/ 
console.log(parimp(223))